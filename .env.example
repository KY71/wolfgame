# Gemini API key(伺服器端;絕不加 NEXT_PUBLIC_ 前綴)
# 沒設定時遊戲會用 Mock 決策(可離線玩,NPC 發言是罐頭)
GEMINI_API_KEY=

# 可選:指定模型,預設 gemini-flash-latest(alias,不會被下架)
# 注意:別釘死版本號如 gemini-2.5-flash——Google 會對新 key 下架該版本,
#      回 404 導致 NPC 全部沉默不語。用 alias 最保險。
# GEMINI_MODEL=gemini-flash-latest
